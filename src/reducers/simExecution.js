
const initialState = {
};

export const simExecution = (state = initialState, action) => {
  switch (action.type)
  {
    // case RECEIVE_REALMS:
    //   if (action.region === "US")
    //   {
    //     return action.json;
    //   }
    //   else
    //   {
    //     return state;
    //   }
    default:
      return state;
  }
};